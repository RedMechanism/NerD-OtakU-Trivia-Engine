<Page x:Class="NOTE.Questions_Page"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:NOTE"
      mc:Ignorable="d" 
      d:DesignHeight="476" d:DesignWidth="724"
      Title="Questions_page">
    <Page.Resources>
        <Style TargetType="DataGridRow" x:Key="OpaqueDataGridRowStyle">
            <Setter Property="Background" Value="White"/>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.Resources>
            <Style TargetType="{x:Type DataGridRow}" x:Key="CustomDataGridRowStyle">
                <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="pink"/>
                        <Setter Property="Foreground" Value="Black"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Grid.Resources>
        <DataGrid x:Name="CategoryGrid" Opacity="0.8"
                  AutoGenerateColumns="False" 
                  RowDetailsVisibilityChanged="MainGrid_RowDetailsVisibilityChanged"
                  ScrollViewer.CanContentScroll="True"
                  ScrollViewer.IsDeferredScrollingEnabled="True"
                  PreviewMouseDown="CategoryGrid_PreviewMouseDown">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Question" Binding="{Binding QuestionName}"/>
                <DataGridTextColumn Header="Team" Binding="{Binding Team.Name}"/>
                <DataGridTextColumn Header="Points" Binding="{Binding Points}"/>
                <DataGridTextColumn Header="Flex" Binding="{Binding BonusPoints}"/>
                <DataGridTextColumn Header="Penalty" Binding="{Binding Penalty}"/>
                <DataGridTextColumn Header="Time" Binding="{Binding Time, StringFormat={}{0:mm}:{0:ss}}"/>
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="{x:Type DataGridRow}">
                    <Setter Property="Background" Value="{Binding RowColor}" />
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="pink"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="pink"/>
            </DataGrid.Resources>
        </DataGrid>
    </Grid>
</Page>
